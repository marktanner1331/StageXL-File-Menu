file menu
	everything a menu item

every menu item can have click listener

menu item
	can have children
	text
	back-color
	text-color
	clickable

has its own class that subclasses menu item
	checkbox
	checkbox-tick-color
	
opens up blocker on top of stage

minimum width - scaling if lower
fixed height (unless scaled)
add seperator
	custom height
	draws thin line using above text-color

addItem returns the item added (unless its a seperator)


